# Day 228 🗃️

**P-228** · 🗃️ **The server cache** — ⭐⭐ server data was never client state

**Time:** 3 hrs · **Mode:** NEW

> ⭐⭐ **The sentence to own: data that lives on your server and is merely displayed in the browser is
> a cache with a staleness policy, not application state — and once you name it correctly, refetching,
> deduplication, invalidation and the loading flicker stop being separate problems and become one
> problem you already know how to reason about.**
>
> Today: ⭐ the reframe, staleness, ⭐⭐ invalidation, and what a query library actually buys.

---

# Part 1 — ⭐⭐ The reframe

```
⭐⭐ TWO KINDS OF DATA, AND THEY NEED DIFFERENT MACHINERY

⭐ CLIENT STATE                   ⭐ SERVER STATE (a CACHE)
⭐  · you own it                    · ⭐⭐ SOMEONE ELSE OWNS IT
⭐  · it is always correct          · ⭐⭐ YOUR COPY GOES STALE
⭐  · synchronous                   · async, can fail, can be slow
⭐  · no other writer               · ⭐ OTHER USERS CHANGE IT
⭐  ⇒ useState                      ⇒ ⭐⭐ A CACHE WITH A TTL AND
⭐  e.g. is the modal open,           AN INVALIDATION STRATEGY
⭐       the form draft,             e.g. the user list, the order,
⭐       which tab is active              anything from your API

⭐   ⇒ ⭐⭐ PUTTING SERVER DATA IN useState IS THE CATEGORY ERROR THAT
⭐     CREATES: duplicate fetches, stale screens after a mutation,
⭐     loading flicker on every back-navigation, and two components
⭐     showing different values for the same row.
```

> ⭐ **You already own this vocabulary.** TTL, staleness, invalidation, cache stampede,
> stale-while-revalidate — Stage 5 and Day 454. **This is that, in a browser.** The only new part is
> that the cache is per-tab and dies on refresh.

---

# Part 2 — Staleness is a setting, not an accident

```jsx
const { data, isPending, isError, error } = useQuery({
  queryKey: ["users", { role }],        // ⭐⭐ THE CACHE KEY
  queryFn: ({ signal }) => api(`/users?role=${role}`, { signal }),
  staleTime: 30_000,                    // ⭐ fresh for 30 s — no refetch
  gcTime: 5 * 60_000,                   // ⭐ keep unused data 5 min
});
```

```
⭐⭐ THE TWO CLOCKS PEOPLE CONFUSE

⭐   staleTime ⇒ ⭐ "how long is this data considered CURRENT?"
⭐               while fresh: ⭐⭐ NO REFETCH AT ALL, even on remount.
⭐   gcTime    ⇒ "how long do I keep it after nothing is using it?"
⭐               ⭐ this is about MEMORY, not correctness.

⭐   ⭐⭐ staleTime: 0 (the default) MEANS EVERY MOUNT REFETCHES.
⭐     That is safe and it is why people say "it fetches constantly".
⭐     ⇒ ⭐ SET IT DELIBERATELY, PER QUERY. A country list can be
⭐       an hour. A live order status is zero.
```

| ⭐ Data | staleTime |
|---|---|
| Reference lists (countries, categories) | ⭐ Hours — it does not change |
| A user profile | ⭐ 1–5 min |
| A dashboard count | ⭐ 30 s |
| Order status, anything live | ⭐⭐ 0 + refetch on focus/interval |

> ⭐⭐ **The killer feature is deduplication.** Five components asking for `["user", 7]` in the same
> tick produce **one** request. Hand-rolled hooks produce five. That alone usually removes a third of
> a page's requests, and it is why the reframe pays for itself immediately.

---

# Part 3 — ⭐⭐ Invalidation

```jsx
const qc = useQueryClient();

const { mutate, isPending } = useMutation({
  mutationFn: (body) => api("/users", { method: "POST", body }),
  onSuccess: () => {
    qc.invalidateQueries({ queryKey: ["users"] });   // ⭐⭐ "this is now stale"
  },
});
```

```
⭐⭐ AFTER A WRITE, YOUR CACHE IS WRONG. THREE OPTIONS.

⭐ ① ⭐⭐ INVALIDATE  — mark stale, refetch what is on screen.
⭐    ⭐ THE DEFAULT. Always correct, costs one request.
⭐ ② WRITE THROUGH  — put the server's response straight into the
⭐    cache (setQueryData). ⭐ Saves the request; only valid if the
⭐    response is the FULL new object.
⭐ ③ ⭐ OPTIMISTIC   — update before the server answers, roll back
⭐    on failure. ⭐⭐ FASTEST AND MOST DANGEROUS — Day 278.

⭐ ⭐⭐ THE HIERARCHICAL KEY IS THE WHOLE DESIGN:
⭐   ["users"]                    invalidating this...
⭐   ["users", { role: "admin" }] ...also invalidates this ✅
⭐   ["users", 7]                 ...and this ✅
⭐   ⇒ ⭐ KEY DESIGN IS CACHE DESIGN. Get the prefixes right and
⭐     invalidation is one line; get them wrong and you are
⭐     invalidating everything or nothing.
```

| ⭐ Bug | Cause |
|---|---|
| Screen stale after a save | ⭐⭐ Forgot to invalidate — **the most common query-library bug** |
| Everything refetches on any write | ⭐ Invalidating `[]` or a too-short prefix |
| Two components disagree | ⭐⭐ Different key shapes for the same data — `["user",7]` vs `["users",7]` |

---

# Part 4 — What you are actually buying

| ⭐ Problem | Hand-rolled (Day 227) | Library |
|---|---|---|
| Loading / error / empty | ✅ you wrote it | ✅ |
| Race + cancel | ✅ you wrote it | ✅ |
| **Dedupe identical requests** | ❌ | ⭐⭐ ✅ |
| **Cache across mounts** (back button) | ❌ | ⭐⭐ ✅ |
| Refetch on window focus / reconnect | ❌ | ✅ |
| **Stale-while-revalidate** (show old, fetch new) | ❌ | ⭐⭐ ✅ **no flicker** |
| Retry with backoff | ⭐ you could | ✅ |
| Pagination / infinite scroll | ❌ | ✅ |
| Devtools showing every cache entry | ❌ | ⭐ ✅ |

> ⭐⭐ **This is the correct shape of a "should we add a dependency?" answer** (Day 221): a list of
> named problems it deletes, and an honest note on what it costs — ~13 KB gzipped, a new mental model,
> and a real chance of misconfiguring `staleTime` and thinking the library is broken.

```
⭐⭐ WHAT IT DOES NOT SOLVE — say this too
⭐   · ⭐ IT IS NOT A STATE MANAGER. Modal open? Still useState.
⭐   · it does not make your API fast
⭐   · ⭐⭐ IT DOES NOT FIX A WATERFALL (Day 227) — it caches the
⭐     round trips, it does not remove the sequence
⭐   · the cache is per-tab and gone on refresh
```

---

## Today's build

1. Convert three hand-rolled fetches to `useQuery`. ⭐ Delete the code you no longer need and count the lines.
2. ⭐⭐ **Prove deduplication.** Render the same query in four components. Confirm **one** request.
3. Set `staleTime: 0`, navigate back and forth, watch the flicker. Set 30 s. ⭐ Watch it vanish.
4. ⭐⭐ **Write the stale-screen bug**: mutate without invalidating. Then fix it. This is the one you will hit in real work.
5. Design keys for your API — ⭐ list, filtered list, detail. Prove one `invalidateQueries` refreshes all three.
6. ⭐ Write down what the library did **not** solve.

---

# 🚪 Exit questions

```
⭐ ① Client state vs server state — give three differences and the
⭐   category error.
⭐ ② staleTime vs gcTime — which is correctness, which is memory?
⭐ ③ Name the three post-write strategies and when each is right.
⭐ ④ Why is key design cache design?
⭐ ⑤ Name three things a query library does NOT solve.
```

---

## 🎙️ Articulation drill

⭐⭐ **Two minutes: "why a query library instead of `useState` plus `useEffect`?"**

Lead with the reframe, because it is the actual insight. **Because server data was never client state.
It's a cache — someone else owns it, my copy goes stale, other users change it underneath me. Once I
name it correctly, refetching, deduplication, invalidation and the loading flicker stop being four
separate problems and become one problem I already know how to reason about, using the same vocabulary
as any cache: TTL, staleness, invalidation.**

Then what it buys, as a list rather than a vibe. **Deduplication — five components asking for the same
user produce one request instead of five. A cache that survives unmount, so the back button is instant
instead of a full reload. Stale-while-revalidate, so I show the old data while fetching and there's no
flicker. And retry, focus refetch, and pagination I'd otherwise write badly.**

Then the part that shows you configured it rather than copied it. **staleTime is a per-query decision,
not a default to leave at zero. A country list can be an hour; a live order status is zero with refetch
on focus. Most "it fetches constantly" complaints are staleTime left at the default.**

Close honestly. **It's not a state manager — modal open is still useState. It doesn't make my API fast,
and it doesn't fix a waterfall; it caches the round trips, it doesn't remove the sequence. And the most
common bug with it is mine, not its: forgetting to invalidate after a write, so the screen is stale
and nothing errors.**

---

**Previous:** [Day 227](Day-227.md) · **Tomorrow:** [Day 229](Day-229.md) — ⭐ **Forms**: controlled vs uncontrolled, ⭐⭐ mirroring your Pydantic schema without duplicating it, and putting the server's 422 next to the right input
