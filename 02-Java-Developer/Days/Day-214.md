# Day 214 🌐

**J-214** · 🌐 **Stage 6 opens** — ⭐⭐ why a Spring engineer learns the frontend, and how much

**Time:** 3 hrs · **Mode:** NEW · **⭐ Stage 6 opens**

> ⭐⭐ **The sentence to own: you are not learning frontend to become a frontend engineer — you are
> learning it because an API whose UI you have never written is an API you have only half designed,
> and because every interview question about your project eventually arrives at the browser.**
>
> Today: the honest reason this stage exists, ⭐⭐ **the four things the browser changes about your
> backend**, the scope of the next 34 days, and what this stage deliberately does *not* teach.

---

# Part 1 — ⭐⭐ Why this stage is not optional

You have spent 213 days on the JVM, Spring and databases. The temptation is to treat the browser as
somebody else's problem. Four reasons that fails.

```
⭐⭐ THE FOUR THINGS THAT ONLY BECOME VISIBLE FROM THE CLIENT SIDE

⭐ ① ***YOUR API SHAPE IS WRONG AND YOU CANNOT SEE IT.***
⭐   A @GetMapping that returns a User, then needs three more calls
⭐   to render one screen, ⭐⭐ IS A GOOD ENDPOINT AND A BAD API.
⭐   You only find out by building the screen.

⭐ ② ***YOUR ERROR MODEL IS UNUSABLE.*** ⭐ A MethodArgumentNot-
⭐   ValidException serialised by default is easy to emit and
⭐   ⭐⭐ IMPOSSIBLE TO PUT NEXT TO THE RIGHT INPUT BOX. The client
⭐   is where you learn which errors need a FIELD NAME attached.

⭐ ③ ***YOUR AUTH DESIGN LEAKS.*** ⭐⭐ WHERE THE TOKEN LIVES IS A
⭐   BROWSER QUESTION, not a Spring Security question — and getting
⭐   it wrong is the single most common security hole in a junior
⭐   project. → Day 235.

⭐ ④ ***YOUR LATENCY IS A LIE.*** ⭐ Actuator says p99 = 40 ms. The
⭐   user waits 2.8 seconds. ⭐⭐ THE GAP IS DNS, TLS, BUNDLE SIZE, A
⭐   WATERFALL OF DEPENDENT REQUESTS, AND RENDER — none of which
⭐   appear in Micrometer. → Day 241.
```

| ⭐ The interview version | |
|---|---|
| **"Walk me through your project"** | Ends at a screen. If you can only describe the API, the story stops halfway and the interviewer notices. |
| **⭐⭐ "Why is this endpoint shaped like that?"** | The strongest answer is *"because the screen needed it in one round trip"* — which you can only say if you built the screen. |
| **"Who did the frontend?"** | ⭐ *"I did"* is worth more than a better Spring answer. It closes the question of whether the project is really yours. |

> **⭐⭐ The claim of this stage, stated honestly.** After 34 days you will be able to build a
> competent, accessible, secure UI for an API you wrote, and defend every decision in it. You will
> **not** be a frontend specialist, you will not out-CSS anyone, and you should not apply for
> frontend roles on the strength of it. That is the correct trade, and saying it out loud in an
> interview is a strength rather than an admission.

---

# Part 2 — What you already know that transfers

Do not start from zero. Most of this stage is a re-labelling of things you own.

| ⭐ You already have | Where it lands in Stage 6 |
|---|---|
| **HTTP, status codes, headers** (Days 010–011) | Unchanged. `fetch` is a thin client over what you already know — with ⭐ one nasty difference on Day 217. |
| **Interfaces and polymorphism** | ⭐⭐ TypeScript is **structurally** typed, not nominally. Your instincts mostly transfer; the `implements` keyword does not. → Day 218 |
| **CORS** (Day 020) | You learned it from the Spring side. Stage 6 shows you the error message the browser actually prints. → Day 237 |
| **Caching and staleness** (Stage 5) | TanStack Query is a cache with a TTL and an invalidation strategy. ⭐ Same vocabulary as your `@Cacheable`. → Day 228 |
| **Bean Validation** (`@Valid`, Stage 4) | The form on Day 229 mirrors your DTO constraints. ⭐⭐ Two validators, one contract, and the drift between them is a real bug class. → Day 246 |
| **DTOs, never entities** (Day 147) | ⭐⭐ Day 236 shows you *why* from the outside: you can see the whole payload in DevTools |
| **Supply chain risk** | ⭐ npm has Maven's problem, worse and faster. → Day 221 |

```
⭐⭐ THE MENTAL MOVE THAT MAKES THIS STAGE EASY

⭐  Stop thinking "a page is a document I generate" (JSP/Thymeleaf).
⭐  Start thinking: ⭐⭐ THE UI IS A PURE FUNCTION OF STATE, and the
⭐  framework's whole job is to re-run it efficiently when state changes.

⭐    render(state) -> what the screen should look like
⭐    ⭐⭐ YOU DESCRIBE THE RESULT. YOU DO NOT DESCRIBE THE STEPS.

⭐  ⇒ every React bug you will hit is really the question
⭐    ⭐⭐ "WHAT IS ACTUALLY STATE, AND WHAT IS DERIVED FROM IT?"
⭐    (Day 225 — and it is the single highest-value idea in the stage.)
```

> ⭐ **The one genuinely new mental model** is single-threaded asynchrony (Day 216). You are used to a
> thread per request and blocking calls being fine. In the browser there is **one** thread, it also
> draws the screen, and blocking it freezes the UI. Virtual threads made blocking cheap on the JVM;
> the browser went the other way.

---

# Part 3 — The 34 days

| Block | Days | What you get |
|---|---|---|
| **The language & platform** | 214–222 | JS that is worth writing, async, `fetch`, ⭐ TypeScript, the DOM, npm, the build step |
| **React** | 223–232 | The model, state, ⭐⭐ effects and the dependency array, data fetching, forms, keys, performance |
| **The app around it** | 233–240 | Routing, rendering strategies, ⭐⭐ auth in the browser, CORS from the other side, styling, accessibility, error states |
| **Shipping & the interview** | 241–247 | Core Web Vitals, images and fonts, ⭐ XSS and CSP, testing, deploys, ⭐⭐ the typed client, the 🚪 gate |

> **⭐ The stage's build.** One UI, grown across the 34 days, against **the Spring Boot API you wrote in
> Stage 4**. Not a tutorial to-do app — your own endpoints, your own auth, your own error shapes. That
> constraint is what makes the stage produce interview material instead of a screenshot.

---

# Part 4 — ⭐⭐ What this stage deliberately refuses to teach

Naming the exclusions is what keeps 34 days from becoming 200.

| ❌ Not here | ⭐ Why |
|---|---|
| **CSS mastery, animation, design** | You are not being hired for it, and a component library plus tokens (Day 238) gets you to "competent and unremarkable", which is the correct target. |
| **Three frameworks** | You learn **React**, because it is what the job market and your interviewer will assume. |
| **Thymeleaf / server-side templating** | ⭐⭐ You may meet it at work; it does not teach you the client-server contract, which is the point of this stage. |
| **State management libraries** | Redux, Zustand. ⭐⭐ Most apps that reach for one had a *derived state* problem (Day 225) or a *server cache* problem (Day 228). Solve those first. |
| **Micro-frontends, module federation** | ⭐ A solution to an org-chart problem you do not have. |

> **⭐⭐ The rule for the whole stage:** every time you are tempted to add a library, ask *"which of my
> problems does this delete, and what does it cost me to explain in an interview?"* That question is
> the same one you have been asking since Stage 2, and it does not change because the code runs in a
> browser.

---

## Today's build

1. **Open your Stage 4 API's Swagger UI.** Pick the **one screen** that would make the project
   demonstrable — the main workflow, not the login.
2. **Write the screen down on paper.** Every field, every button, every state it can be in.
3. ⭐⭐ **List the API calls that screen needs.** If it is more than two, write down why — you have just
   found your first real API design finding, before writing a line of frontend.
4. **Write the four states it must handle**: loading, empty, error, success. ⭐ Most projects implement
   one of the four, and it is always "success".
5. Record which of the four your API currently makes *possible* to render. A 500 with a Whitelabel
   Error Page HTML body is not an error state you can render.

---

# 🚪 Exit questions

```
⭐ ① Give the four things that only become visible from the client
⭐   side. Which one is a security issue?
⭐ ② What is the honest scope claim of this stage — and the honest
⭐   thing it does NOT make you?
⭐ ③ "The UI is a pure function of state." What question does that
⭐   turn every React bug into?
⭐ ④ Why does this stage refuse to teach a state management library
⭐   up front?
⭐ ⑤ Your p99 is 40 ms and the user waits 2.8 s. Name three places
⭐   the time went that Micrometer cannot see.
```

---

## 🎙️ Articulation drill

⭐⭐ **Two minutes, out loud: "you're a backend engineer — why did you build the frontend yourself?"**

Lead with the API argument, because it is the one that sounds like engineering rather than ambition.
**An API whose UI I have never written is an API I've only half designed. The first screen I built
needed four round trips to render one view — each endpoint was individually correct and the API was
wrong, and I could not have seen that from the server side.** Then the error model: **my validation
failure serialised into a shape that was easy for Spring to emit and impossible to attach to the right
input box, so I changed the contract to carry a field name.**

Then be explicit about scope, because over-claiming here is how people lose the round. **I'm not a
frontend engineer. I can build a competent, accessible, secure UI for an API I wrote and defend every
decision in it — I'm not going to out-CSS anyone, and I wouldn't apply for a frontend role on the
strength of it.** That sentence tends to *increase* trust rather than reduce it.

Close on what it changed in your backend, which is the point. **Building the client is where I learned
that where the token lives is a browser decision and not a Spring Security one, and that my p99 of
forty milliseconds had nothing to do with what the user experienced. Both of those are things I now
design for on the server, and neither was visible from where I was standing.**

---

**Previous:** [Day 213](Day-213.md) — Stage 5 exit gate · **Tomorrow:** [Day 215](Day-215.md) — ⭐ **JavaScript, the parts worth writing**: the language as it actually is in 2026, and ⭐⭐ the two kinds of null that your Jackson serialiser is about to produce
